# Provenance

This example adapts the game contract from an internal Freesolo environment.

The implementation is rewritten for strict JSON turns, deterministic inline data, current Freesolo environment APIs, and Flash structured rollout configuration.
